import FileUpload from '.'

describe('InputLabel Unit testing..', () => {
  it('is truthy', () => {
    expect(FileUpload).toBeTruthy()
  })
  it('it should select a file')
  it('it shouldnt select a file when disabled')
})
