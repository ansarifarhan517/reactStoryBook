import styled from 'styled-components'

const ColumnFilterLoading = styled.div`
  width: 100%;
  height: 25px;
  border-bottom: 2px solid ${({ theme }) => theme?.colors?.grey?.['400']}55;
`

export default ColumnFilterLoading
