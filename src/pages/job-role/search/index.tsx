import React from 'react'
import { PageHeader } from '@components'

const Search: React.FC = () => (
  <PageHeader title="Job Roles" btnLabel="Add job" redirectUrl="/job-role/create" />
)

export default Search
