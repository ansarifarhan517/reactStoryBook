import React from 'react'
import { PageHeader } from '@components'

const Search: React.FC = () => (
  <PageHeader title="Events" btnLabel="Add events" redirectUrl="/event/create" />
)

export default Search
